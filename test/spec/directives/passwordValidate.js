'use strict';

describe('Directive: passwordValidate', function () {
  beforeEach(module('selfApp'));

  var element;

  it('should correctly identify valid passwords',
     inject(function ($rootScope, $compile, viewValue) {
       viewValue = "lol123";
       expect
     }));

  it('should correctly idenfity invalid passwords',
     inject(function ($rootScope, $compile) {

     }));
});
